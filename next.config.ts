import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // disabled temporarily to avoid typecheck errors on newly created routes
  // typedRoutes: true
};

export default nextConfig;
